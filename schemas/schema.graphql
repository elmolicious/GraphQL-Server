type User {
	email: ID!
	firstName: String!
	lastName: String!
	accountIds: [Integer]!
}

type Account {
	id: ID!
	name: String!
	country: Country!
	users: [User!]
}

enum Country {
	GERMANY
	USA
}

type Query {
	getUser(id: ID!): User
	getAllUsers: [User]

	getUserBatch(ids: [ID!]!): [User]

	getAccount(id: ID!): Account
	getAllAccounts: [Account]
}
